SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/fintrack
SPRING_DATASOURCE_USERNAME=fintrack
SPRING_DATASOURCE_PASSWORD=fintrack
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379

# Generate with: openssl rand -base64 64
APP_JWT_SECRET=
APP_JWT_EXPIRY_MINUTES=60

# From Mailgun dashboard: Account > Security > API Keys
MAILGUN_API_KEY=
# Sending domain, or your sandbox domain for dev
MAILGUN_DOMAIN=
